# V1.1.0 测试清单

测试版本：v1.1.0  
测试日期：待填写  
测试环境：Windows / Chrome / GitHub Pages  
测试结果：待填写

## 核心验收

关键功能不通过时暂缓发布。

### 1. 基础输入与保存

- [x] 页面打开后主文本框自动获得焦点。
- [x] 输入文字后立即自动保存。
- [x] 删除文字后立即自动保存。
- [x] 输入换行后立即自动保存。
- [x] 粘贴整段多行文本后完整保存。
- [x] 刷新页面后文本不丢失。
- [x] 关闭并重新打开后文本不丢失。
- [x] 长线事项备忘独立保存。
- [x] 上周随记可以查看和编辑。
- [x] 文本编辑支持浏览器原生 `Ctrl+Z`。
- [x] 模拟 LocalStorage 写入失败时显示明确提示。（Codex 模拟验证通过）
- [x] 写入失败时不能错误显示“已保存”。（Codex 模拟验证通过）
- [x] 写入失败时文本框中的内容仍然保留。（Codex 模拟验证通过）

### 2. 日期解析

- [x] 今天、明天、后天、大后天解析正确。
- [x] 本周五、下周一解析正确。
- [x] 相对日期转换后光标位于完整日期末尾。
- [x] `6.20 任务` 解析正确。
- [x] `6月20日 任务` 解析正确。
- [x] 日期标题可以向后续任务继承。
- [x] 空行后停止继承上一日期域。
- [x] `//` 后的备注不生成提醒。
- [x] 12 月底输入 `1.5 任务` 时识别为次年日期。（Codex临时referenceDate模拟通过）
- [x] 跨年测试可通过临时模拟 `referenceDate` 完成，测试代码不得提交。

### 3. 无效日期与时间

- [x] `2.30 测试` 不生成错误日期任务。
- [x] `4.31 测试` 不生成错误日期任务。
- [x] `6.20 25:60 测试` 中 `25:60` 不被识别为有效时间。
- [x] `6.20 24:00 测试` 中 `24:00` 不被识别为有效时间。
- [x] `6.20 00:00 测试` 中 `00:00` 被识别为当天零点。
- [x] 无效输入不会造成页面报错或白屏。

### 4. 日期域与空任务

测试输入：

```text
6.20：
测试
23:00 发布
下午3点 开会
晚上8点半 检查结果
```

- [x] `6.20：` 本身不生成空任务。
- [x] 测试继承 6.20，且没有具体时间。
- [x] 发布继承 6.20，时间为 23:00。
- [x] 开会继承 6.20，时间为 15:00。
- [x] 检查结果继承 6.20，时间为 20:30。
- [x] 时间文本不会残留在任务标题中。
- [x] 单独输入 `下午3点` 不生成无标题任务。
- [x] `6.20 23:00 发布` 原有写法仍然正常。

### 5. 项目前缀

- [x] 中文冒号结尾的标题可以作为项目上下文。
- [x] 英文冒号结尾的标题可以作为项目上下文。
- [x] 后续任务正确显示项目上下文前缀。
- [x] 新项目标题可以替换上一项目上下文。
- [x] 上下文标题本身不生成任务。

### 6. 右侧显示与分组

- [x] 日期格式为“日期 · 星期几”。
- [x] 有具体时间时显示“日期 · 星期几 · 时间”。
- [x] 今日任务不重复显示“今天”。
- [x] 非逾期任务不显示状态文案。
- [x] 已逾期只出现一次。
- [x] 已逾期显示为红色。
- [x] 今日、本周、未来分组正确。
- [x] 小窗和电脑分屏状态下仍能正常查看和勾选。

### 7. 完成状态与历史

- [x] 勾选任务后记录完成状态，并保存 `completedAt` 标记时间戳。
- [x] 今天勾选后仍保留在今日底部。
- [x] 已完成任务只显示“已完成”，不显示“处理于 HH:mm”。
- [x] 取消勾选后恢复为未处理。
- [x] 再次勾选后使用最新标记完成时间戳。
- [x] 早于今天标记完成的任务进入历史。
- [x] 历史按照本周、上周、更早周次分组。
- [x] 没有 `completedAt` 的旧任务仍能正常展示。（Codex 旧数据模拟通过）
- [x] 刷新页面后勾选状态不丢失。

### 7.1 V1.0.1 反向定位回归

- [x] 普通短行定位后，目标行完整出现在可视区域，并将光标准确定位到该行。
- [x] 自动换行长行定位后，textarea 内部滚动到目标文字附近，且不保留整行文本选区。
- [x] 文本开头、中部、末尾定位正常。
- [x] 内容相同但位于不同位置的重复行按 `lineIndex` 定位到对应行。
- [x] 上周随记抽屉打开后可定位到目标行。
- [x] 长线事项备忘展开后可定位到目标行。
- [x] 小窗口下定位后，目标编辑区先进入当前窗口可视范围。

### 8. 待跟进事项

- [x] `>同事A 6.20 15:00 确认反馈意见` 正确解析。
- [x] 正确提取等待对象。
- [x] 正确提取日期和时间。
- [x] 正确提取待跟进内容。
- [x] 今日待跟进按照等待对象分组。
- [x] 已逾期待跟进显示正确。
- [x] 复制今日待跟进只包含未处理且已经到期的事项。
- [x] 复制内容保留项目上下文。

### 9. 提醒

- [x] 无具体时间的任务不会触发到点提醒。
- [x] 有具体时间的任务到点后触发提醒。
- [x] 已完成任务不会再次提醒。
- [x] 拒绝通知权限后应用不会报错。
- [x] 系统通知不可用时页面内提示正常。
- [x] 在浏览器设置中重新开启权限后，刷新页面可恢复通知。

### 10. JSON、备份与恢复

- [x] 导出 JSON 备份成功。
- [x] 备份包含便签和任务状态。
- [x] 正常备份可以完整恢复。
- [x] JSON 语法错误时拒绝导入。（Codex 模拟验证通过）
- [x] JSON 顶层为数组时拒绝导入。（Codex 模拟验证通过）
- [x] JSON 顶层为 `null` 时拒绝导入。（Codex 模拟验证通过）
- [x] value 不是字符串时拒绝导入。（Codex 模拟验证通过）
- [x] taskStates 结构错误时拒绝导入。（Codex 模拟验证通过）
- [x] extracted 数据不是数组时拒绝导入。（Codex 模拟验证通过）
- [x] 无效备份不会覆盖现有数据。（Codex 模拟验证通过）
- [x] 写入中途失败时不会留下部分恢复结果。（Codex 模拟验证通过）
- [x] 导入失败不会造成白屏。（Codex 模拟验证通过）

### 11. PWA、离线和发布页面

- [x] 首次在线访问后，断网仍可打开页面。
- [x] GitHub Pages 可以访问最新版。
- [x] 修改缓存版本后可以加载新代码。
- [x] `Ctrl+F5` 后不会长期停留在旧版本。
- [x] PWA 安装后可以正常打开。

### 12. 跨周刷新

- [x] 周一刷新页面后进入新一周随记。
- [x] 新一周主文本框使用新的存储内容。
- [x] 旧周内容可以从“上周随记”查看。
- [x] 上周任务状态没有丢失。
- [x] 长线事项备忘不受周次切换影响。
- [x] 可通过周日 23:59 与周一 00:01 的临时模拟完成测试，模拟代码不得提交。（Codex模拟验证通过）
- [ ] 真实周一进行一次简单复核。

### 13. V1.1 首次使用增强

- [ ] 主输入框空白时显示简短多行示例，且不预填真实文本。
- [ ] 长线事项备忘 placeholder 说明带日期内容也会进入提醒。
- [ ] 没有任何有效输入时，右侧显示“还没有日期提醒”说明。
- [ ] 已有文字但没有日期任务时，右侧显示“暂未识别到日期节点”说明。
- [ ] 纯空格和空行按“没有有效输入”处理。
- [ ] 有历史已完成任务但当前没有活动任务时，不误导用户以为当前文本已识别。
- [ ] 输入带日期事项后，空状态立即消失。
- [ ] 删除日期导致当前无任务时，空状态正确切换。
- [ ] 不存在单独展开式“输入示例”区域。
- [ ] placeholder 示例不写入 textarea 真实 value。
- [ ] placeholder 示例不进入 LocalStorage、解析器或备份。
- [ ] 360px 窄窗口下 placeholder 正常换行，不产生横向滚动。

### 14. V1.1 已完成复制

- [ ] 未完成事项不显示单条复制入口。
- [ ] 已完成事项显示单条复制入口。
- [ ] 单条复制不会触发任务正文定位。
- [ ] 单条复制包含项目上下文、任务标题和计划日期。
- [ ] 单条复制不包含“已完成”、`completedAt`、内部 ID、来源键或备注字段。
- [ ] 复制成功使用页面 Toast。
- [ ] Clipboard API 不可用时 fallback 可用。
- [ ] 复制失败时显示简短失败提示。
- [ ] 复制按钮可键盘访问，且有准确 `aria-label`。

### 15. V1.1 按周复制已完成

- [ ] 复制本周已完成按任务计划日期判断本周。
- [ ] 复制上周已完成按任务计划日期判断上周。
- [ ] 周次判断不使用 `completedAt`。
- [ ] 普通任务使用 `dueDate`。
- [ ] 待跟进任务使用 `followUpDate`。
- [ ] 数据来源覆盖本周随记、上周随记、长线事项备忘和其他已存储历史周。
- [ ] 不只复制当前可见列表。
- [ ] 相同任务不重复输出。
- [ ] 按项目上下文分组。
- [ ] 无项目任务进入“其他事项”。
- [ ] 组内按计划日期升序。
- [ ] 输出不重复项目上下文前缀。
- [ ] 输出不包含精确完成时间和 `completedAt`。
- [ ] 目标周没有已完成事项时，不复制空模板，并显示 Toast。

### 16. V1.1 固定上下布局

- [ ] 记录区约占 55%，DDL 区约占 45%。
- [ ] 页面内重复大标题已压缩或移除，只保留紧凑日期和状态。
- [ ] 主输入框保持最低可用高度，不会被压缩成几行。
- [ ] 长线事项展开不再压缩主输入框。
- [ ] 两个区域之间只显示低视觉权重的普通分界线。
- [ ] 不存在拖动手柄、横杠、圆点或 `row-resize` 光标。
- [ ] 记录区内部滚动正常。
- [ ] DDL 区内部滚动正常。
- [ ] textarea 保留自己的文本滚动。
- [ ] 输入正文约 16px，高 DPI 下文字清晰。
- [ ] 较矮窗口下不产生负高度、重叠或内容泄漏。
- [ ] 长文本不撑破布局。
- [ ] 长任务标题正常换行。
- [ ] 刷新后布局保持固定，不依赖 LocalStorage。
- [ ] 旧 `workspace_layout_ratio` 值存在时，页面仍使用固定布局。
- [ ] 点击任务定位仍能让目标编辑区和目标行可见。
- [ ] 主页面 DDL 底部“查看历史已完成（数量）”和“复制本周已完成”保持同行。
- [ ] 底部“复制本周已完成”的右边缘与顶部“复制今日待跟进”的右边缘处于同一竖列。
- [ ] 底部复制按钮右侧保留与顶部数量徽标等宽的空列，不与数量徽标对齐。

### 17. V1.1 响应式和兼容性

- [ ] 360px 宽无横向滚动。
- [ ] 420px 宽无横向滚动。
- [ ] 520px 宽无横向滚动。
- [ ] 较矮窗口下记录区和提醒区都可用。
- [ ] 正常桌面窗口保持单列布局。
- [ ] 高 DPI Windows 显示缩放下按钮和文本不重叠。
- [ ] Chrome 可用。
- [ ] 已安装 PWA 可用。
- [ ] GitHub Pages 环境可加载新缓存版本。
- [ ] 长标题正常换行，复制按钮不遮挡标题。
- [ ] 上周抽屉、历史抽屉、长线事项、自动保存、中文输入法、Ctrl+Z 和相对日期替换光标正常。

## 无文档首次上手测试

目标：完全陌生的用户不读 README，也能在一分钟内独立完成“输入带日期事项 → 看见提醒 → 点击返回原文 → 勾选完成”的核心链路。

测试任务：

- [ ] 记录“明天下午三点开会”。
- [ ] 记录一个需要等待同事回复的事项。
- [ ] 从右侧找到对应任务并返回原文修改。
- [ ] 勾选完成。
- [ ] 复制一条已完成事项。
- [ ] 观察是否能从 placeholder 理解基本写法。
- [ ] 观察固定上下布局是否足够理解和使用。

观察项：

- [ ] 是否直接进入输入框。
- [ ] 是否理解需要日期。
- [ ] 第一次输入后是否看到提醒。
- [ ] 是否理解右侧内容来自左侧。
- [ ] 是否会点击任务正文。
- [ ] 是否能发现待跟进写法。
- [ ] 是否能从 placeholder 发现基本写法。
- [ ] 是否能理解“长线事项备忘”。
- [ ] 是否能找到复制入口。
- [ ] 是否能理解上下两个区域分别用于记录和提醒。
- [ ] 卡住时第一反应是什么。
- [ ] 是否把“已保存”误认为“已识别”。

不要把“觉得不错”“挺好用”等礼貌反馈写成验收结论。

## 防御性测试

以下项目不阻塞 V1.0。未通过时记录为已知限制。

### 1. 页面长期不刷新

- [x] 页面跨到第二天后日期状态是否自动更新。
- [x] 页面从周日跨到周一后是否自动切换周次。
- [x] 如不支持，记录“页面跨日或跨周长期保持打开时，建议刷新页面以更新日期和周次”。

### 2. 时间与 UI 实时状态

- [x] 页面保持打开时，到达具体时间后是否触发提醒。
- [x] 提醒触发后页面不会报错。
- [x] 同一天具体时间过去后不强制标红。
- [x] 已逾期仍按照日期早于今天判断。

### 3. 性能

- [ ] 粘贴约 1—2 万字后页面不崩溃。（未测，非阻断）
- [x] 粘贴约 200—500 行后能够完成保存和解析。
- [x] 大文本下滚动没有严重卡死。
- [x] 连续输入或长按回车不会导致页面崩溃。
- [x] 轻微延迟可记录为已知限制，不必因此改造成复杂编辑器。

### 4. 存储异常

- [x] 通过模拟异常验证保存失败提示。（Codex 模拟验证通过）
- [x] 保存失败提示不会连续刷屏。（Codex 模拟验证通过）
- [x] 保存失败后仍可点击导出备份。（Codex 模拟验证通过）

## V1.0 已知限制

- 提醒只在网页或 PWA 打开期间运行。
- 数据只保存在当前浏览器 LocalStorage。
- 清理浏览器数据、换浏览器或换设备不会自动同步。
- 页面跨日或跨周长期不刷新时，可能需要手动刷新。
- 不支持自动发送邮件。
- 不支持自动检测邮件回复。
- 不支持回车投递模式。
- 不支持云同步。
- 不支持多人协作。
- `24:00` 不作为合法时间，次日零点需写次日日期加 `00:00`。
